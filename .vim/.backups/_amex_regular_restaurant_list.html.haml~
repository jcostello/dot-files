- @restaurants_presenter.general_restaurants.each do |restaurant|
  .other-restaurant.clearfix
    .description
      .header
        .subtitle
          %p.location
            = restaurant.zone.name if restaurant.zone
          %p.food-type
            = restaurant.cuisines.first.name
        .title
          %h3.name 
            = restaurant.name
          %span.price 
            = "$$$" #TODO: MAKE THIS DYNAMIC
      %p 
        = restaurant.description
      %p 
      = link_to "Ver en mapa", "#detalle", class: 'view-map general-reservation', 'data-restaurantid' => restaurant.id, lat: restaurant.lat_long[0], long: restaurant.lat_long[1]
      - if restaurant.take_reservations?
        = link_to "Reservar", "#widget", class: 'btn', rel: 'facybox'
      - else
        .phone
          %p.text Para hacer su reserva por favor llame a conserjería
          %p.number 5879-7489
    .image
      = image_tag restaurant.default_photo_path(:portrait1)
